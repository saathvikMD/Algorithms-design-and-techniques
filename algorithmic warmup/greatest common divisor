# Uses python3

def gcd(a, b):
    d = min(a,b)
    c = max(a,b)
    while (c % d ) != 0:
        min1 = d
        d = c % d
        c = min1
    return d

n = input()
token = n.split()

print(gcd(int(token[0]), int(token[1])))
